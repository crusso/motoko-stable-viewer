{
  "name": "motoko-stable-viewer",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "src/viewer_frontend"
  ],
  "scripts": {
    "build": "npm run build --workspace=viewer_frontend",
    "start": "npm run start --workspace=viewer_frontend"
  },
  "devDependencies": {
    "dotenv": "^17.2.4"
  }
}
